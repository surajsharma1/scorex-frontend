# Vitest Setup Fix Plan

## Tasks
- [x] 1. Fix vite.config.ts - Remove incorrectly placed test setup code
- [x] 2. Fix src/test/setup.ts - Replace Jest globals with Vitest globals
- [x] 3. Fix package.json - Add missing testing dependencies and test script
- [x] 4. Run npm install to install new dependencies
- [x] 5. Verify tests can run (User needs to run: cd scorex-frontend/scorex-frontend && npm install && npm test)

