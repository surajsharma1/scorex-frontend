# Nixpacks configuration for Render deployment
# Point to the nested backend folder

[phases.setup]
nixPkgs = ["nodejs_22", "typescript"]

[phases.build]
cmds = ["cd scorex-backend && rm -rf node_modules package-lock.json && npm install --legacy-peer-deps && npm run build"]

[start]
cmd = "cd scorex-backend && npm start"
